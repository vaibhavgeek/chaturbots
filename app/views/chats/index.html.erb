




<div class="row container top-margin-20">
		<div class="col s6 white no-padding">
	<div class="row">		
<h5> Unread Messages </h5>
	
       <% @vis.each do |visitor| %>
	
	<% if  visitor.name %>
	<% disp = visitor.name %>
	<% elsif visitor.email %>
	<% disp = visitor.email %>
	<% else %>
	<% disp = visitor.location %>
<% end %>
		<div id="<%= visitor.id %>">
		<li class="<%= active_helper(organisation_chat_path(:organisation_id => request.params[:organisation_id] , :id => visitor.id)) %>">
			<%= link_to organisation_chat_path(:organisation_id => request.params[:organisation_id] , :id => visitor.id) , :class => active_helper(organisation_chat_path(:organisation_id => request.params[:organisation_id] , :id => visitor.id)) , :remote => "true" do %>

			<%= truncate(disp, :length => 15) %>

			<% end %>

<span class="hidden"><%= visitor.auth_token %></span>


<% end %> </li>

			  </div> </div> <br/>

<h5> Previous Messages </h5>
				<div class="nav">
				<nav role="navigation">
<ul>
       <% @vis.each do |visitor| %>
	
	<% if  visitor.name %>
	<% disp = visitor.name %>
	<% elsif visitor.email %>
	<% disp = visitor.email %>
	<% else %>
	<% disp = visitor.location %>
<% end %>
		<div id="<%= visitor.id %>">
		<li class="<%= active_helper(organisation_chat_path(:organisation_id => request.params[:organisation_id] , :id => visitor.id)) %>">
			<%= link_to organisation_chat_path(:organisation_id => request.params[:organisation_id] , :id => visitor.id) , :class => active_helper(organisation_chat_path(:organisation_id => request.params[:organisation_id] , :id => visitor.id)) , :remote => "true" do %>

			<%= truncate(disp, :length => 15) %>

			<% end %>

<span class="hidden"><%= visitor.auth_token %></span>
</li>
</div>

<% end %> </ul> </nav>

			  </div>	
		</div>
		<div class="col s6 white no-paading">
		<h5> <center> Chat Archives </center></h5>

		<div id="display_chats" class="display_chats">
	
</div>

		
	</div>
</div>





